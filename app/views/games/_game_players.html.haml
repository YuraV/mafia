.game_players
  - @game.players.each do |player|
    .span2.well
      %br
        %b name:
        = "#{player.user.name}"
      %br
        %b role:
        = "#{player.role}"
      %br
        %b score:
        = "#{player.score}"
      %br
        %b number:
        = "#{player.player_number}"
